apiVersion: azure.upbound.io/v1beta1
metadata:
  name: azure
kind: ProviderConfig
spec:
  credentials:
    source: Secret
    secretRef:
      namespace: upbound-system
      name: azure-secret
      key: creds
